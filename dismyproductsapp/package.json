{
  "name": "dismyproductsapp",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "main": "../../node-3-afternoon/index.js",
  // "main" points to the server file (index.js)
  "nodemonConfig": {
    "ignore": [
      "src/*",
      "public/*"
    ]
  },
  "proxy": "http://localhost:3000"
}

// proxy should point to the port of what the server is running on
